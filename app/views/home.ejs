
<%- include('partials/header') %>

    <div class="pokemon__list__container">
        <% for(let pokemon of pokemons) {%>
            <div class="pokemon__card">
                <a href="/detail/<%= pokemon.id%>">
                    <img class="pokemon__card__img" src="img/<%= pokemon.numero%>.png">
                    <p class="pokemon__card__name">
                        #<%= pokemon.numero%> <%= pokemon.nom%>
                    </p>
                </a>
            </div>

        <%}%>

    </div>
</main>
</body>
</html>
